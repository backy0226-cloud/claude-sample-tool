# claude-sample-tool

## 使い方

1. このリポジトリの `index.html` をブラウザで開きます。
2. 「計算する」ボタンをクリックします。
3. 画面に `1 + 2 = 3` と表示されます。

## 補足

- このサンプルは、外部ライブラリを使わない素の HTML / JavaScript で動作します。
- HTML・CSS・JavaScript を `index.html` 1枚にまとめています。
